{
  "name": "cronometro",
  "version": "1.0.0",
  "description": "cronometro com html, css e javascript",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "rmdir /s /q build && mkdir build && copy index.html build\\ && copy style.css build\\ && copy scripts.js build\\"
  },
  "author": "Lucas Silva",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}